[project]
name = "flask-mongo-chatgpt"
version = "0.1.0"
description = "Flask + MongoDB + ChatGPT Service"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "pymongo>=4.6.0",
    "openai>=1.12.0",
    "python-dotenv>=1.0.0"
]

[tool.vercel]
entrypoint = "run:app"
